[pytest]
# The unit-test suite lives in tests/. scripts/ contains standalone scripts
# (some named test_*.py with stale imports / sys.exit) that are NOT pytest tests,
# so restrict default collection to tests/ and drop the experiment scripts there.
testpaths = tests
addopts = --ignore=tests/experiments
